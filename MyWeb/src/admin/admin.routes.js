(function(){
	'use strict';

	angular.module('admin')
	.config(config);

	config.$inject = ['$stateProvider', '$httpProvider'];
	function config($sateProvider, $httpProvider){
		$httpProvider.intere
	}
})()